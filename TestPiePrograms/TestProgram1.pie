(claim id-Nat (Pi (x Nat) Nat))
(define id-Nat (lambda (x) x))

(claim two (Pi (n Nat) Nat))
(define two (lambda (n) (add1 (add1 n))))
(check-same Nat (two zero) (add1 (add1 zero)))

(claim tt Trivial)
(define tt sole)
(check-same Trivial tt tt)

(id-Nat (add1 zero))
